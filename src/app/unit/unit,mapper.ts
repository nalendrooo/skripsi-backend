// import { DTOCategoryPrisma } from "./category.dto"

// export const mapperExplore = (data: DTOCategoryPrisma[]) => {
//     return data.map((item) => ({
//         slug: item.slug,
//         title: item.title,
//         sub_category: item.SubCategories.map((subCategory) => ({
//             slug: subCategory.slug,
//             title: subCategory.title,
//             topic: subCategory.Topics.map((topic) => ({
//                 slug: topic.slug,
//                 title: topic.title,
//             }))
//         }))
//     }))
// }